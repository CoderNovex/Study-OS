const $=s=>document.querySelector(s);const tasks=[{text:'Review today’s school work',done:false},{text:'Practice one difficult topic',done:false},{text:'10-minute active recall',done:false}];let seconds=0,timer=null;function render(){const box=$('#tasks');box.innerHTML='';tasks.forEach((t,i)=>{const row=document.createElement('label');row.className='task '+(t.done?'done':'');row.innerHTML=`<input type="checkbox" ${t.done?'checked':''}><span>${t.text}</span>`;row.querySelector('input').onchange=e=>{t.done=e.target.checked;render()};box.appendChild(row)});const done=tasks.filter(t=>t.done).length;$('#taskCount').textContent=`${done} / ${tasks.length}`;$('#progress').textContent=`${Math.round(done/tasks.length*100)}%`;}function tick(){seconds++;$('#focusTime').textContent=new Date(seconds*1000).toISOString().substring(14,19)}$('#date').textContent=new Date().toLocaleDateString(undefined,{weekday:'long',month:'long',day:'numeric'});const h=new Date().getHours();$('#greeting').textContent=h<12?'Good morning. Ready?':h<18?'Good afternoon. Let’s lock in.':'Good evening. One focused session.';render();$('#focusBtn').onclick=()=>{if(timer){clearInterval(timer);timer=null;$('#focusBtn').textContent='Resume Focus ⏱️'}else{timer=setInterval(tick,1000);$('#focusBtn').textContent='Pause Focus ⏸️'}};$('#addTask').onclick=()=>{const text=prompt('Task for today:');if(text?.trim()){tasks.push({text:text.trim(),done:false});render()}};document.querySelectorAll('.subject').forEach(b=>b.onclick=()=>{const subject=b.dataset.subject;tasks.push({text:`Study ${subject}`,done:false});render()});